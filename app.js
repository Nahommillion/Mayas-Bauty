const dateInput=document.querySelector('input[type="date"]'); if(dateInput) dateInput.min=new Date().toISOString().slice(0,10);
function submitBooking(e){e.preventDefault();document.getElementById('book-message').textContent='✨ Appointment request prepared successfully. Online database + Telegram synchronization will be connected in the next stage.';}
document.querySelectorAll('.filters button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(b=>b.classList.remove('chosen'));btn.classList.add('chosen');const cat=btn.dataset.cat;document.querySelectorAll('.gallery-item').forEach(x=>x.style.display=(cat==='all'||x.dataset.cat===cat)?'block':'none');}));
const files=document.getElementById('owner-files'); const note=document.getElementById('upload-note');
files?.addEventListener('change',()=>{if(!files.files.length)return;let stored=[];try{stored=JSON.parse(localStorage.getItem('mayaOwnerGallery')||'[]')}catch(e){} [...files.files].forEach(file=>{const r=new FileReader();r.onload=()=>{stored.push({name:file.name,data:r.result});localStorage.setItem('mayaOwnerGallery',JSON.stringify(stored.slice(-20)));note.textContent=`✨ ${files.files.length} picture(s) added to this browser's Maya gallery.`};r.readAsDataURL(file)});});
document.querySelector('.hamburger')?.addEventListener('click',()=>{const nav=document.querySelector('.topbar nav');nav.style.display=nav.style.display==='flex'?'none':'flex';nav.style.position='absolute';nav.style.top='70px';nav.style.left='0';nav.style.right='0';nav.style.padding='14px';nav.style.background='#070604';nav.style.flexDirection='column';});
